import { subcommitteeLink } from 'legislator-lookup/helpers/subcommittee-link';
import { module, test } from 'qunit';

module('Unit | Helper | subcommittee link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subcommitteeLink([42]);
  assert.ok(result);
});
